name: CI/CD
on:
  - push

jobs:
  build:
  label_issue:
    runs-on: ubuntu-latest
    steps:
      - run: |
          wget https://github.com/nesbox/TIC-80/releases/download/v1.1.2837/tic80-v1.1-linux.deb
